import { FirstSection } from '@/components/forgot-password';
import React from 'react';

const ForgotPassword = () => {
	return (
		<main>
			<FirstSection />
		</main>
	);
};

export default ForgotPassword;
