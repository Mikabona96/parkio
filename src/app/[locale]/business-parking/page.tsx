import { FirstSection, SecondSection } from '@/components/business-parking';
import React from 'react';

export default function BusinessParking() {
	return (
		<main className="mt-[4.8rem] flex flex-col gap-[120px]">
			<FirstSection />
			<SecondSection />
		</main>
	);
}
