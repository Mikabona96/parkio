import { FirstSection } from '@/components/reset-password';
import React from 'react';

const ResetPassword = () => {
	return (
		<main>
			<FirstSection />
		</main>
	);
};

export default ResetPassword;
