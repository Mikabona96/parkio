import { FirstSection } from '@/components/contact-us';
import React from 'react';

const ContactUs = () => {
	return (
		<main className="p-[120px]">
			<FirstSection />
		</main>
	);
};

export default ContactUs;
