import { FirstSection, SecondSection } from '@/components/parkio-solutions';

export default function ParkioSolutions() {
	return (
		<main className="relative mt-[4.8rem] flex w-full flex-col gap-[120px]">
			<FirstSection />
			<SecondSection />
		</main>
	);
}
